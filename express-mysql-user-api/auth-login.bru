meta {
  name: auth/login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/users/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@test.com",
    "password": "123456"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.token)
}

settings {
  encodeUrl: true
  timeout: 0
}
