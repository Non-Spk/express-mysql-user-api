meta {
  name: users/delete-user
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/users/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
